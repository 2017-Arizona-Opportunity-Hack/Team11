/*
  getNewList():
    - grab data from IN
    - format for queries
    - run query/queries
    - return JSON list

  onload = refresh.click = getNewList()
*/

function getNewList() {
  // grab data from IN

  // format for queries

  // run query/queries

  // return JSON list
}

window.onload = function() {
  getNewList();
  document.getElementById("refresh_button").onclick = getNewList;
};
